const generateIconPrompt = (title: string) => {
  return `A 3D-rendered digital image of ${title}, shown in soft lighting with realistic shading, clean and minimal design, isolated object centered in frame, on a transparent background.`;
};

export default generateIconPrompt;
